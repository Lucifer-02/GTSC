# Create a simple mail client and mail server
- Main code: `client.c` and `server.c`
- Main idea: in `python/` directory
- The `test/` directory just for development
- The `accounts.txt` file is accounts database
